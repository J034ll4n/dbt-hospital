hospital:
  outputs:
    dev:
      type: snowflake
      account: fcizuif-bw53205
      user: "{{ env_var('DBT_ENV_USER') }}"
      password: "{{ env_var('DBT_ENV_PASSPHRASE') }}" 
      
      role: TRANSFORM
      database: HOSPITAL_DB
      schema: DEV
      threads: 1
      warehouse: COMPUTE_WH
  target: dev